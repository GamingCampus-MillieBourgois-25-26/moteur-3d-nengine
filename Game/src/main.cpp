#include "Application.h"

int main()
{
    // Crée une instance de ton moteur
    Engine::Application app;

    // Lance le moteur (pour l’instant ça ouvre juste la MessageBox)
    app.Run();

    return 0;
}
